<Application x:Class="Poe2DesktopClock.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <!-- Wraeclast-inspired palette: soot, aged bronze, parchment and ember. -->
        <Color x:Key="WindowBackgroundColor">#0B0A08</Color>
        <Color x:Key="PanelBackgroundColor">#17130F</Color>
        <Color x:Key="RaisedPanelColor">#211B14</Color>
        <Color x:Key="InputBackgroundColor">#0D0B09</Color>
        <Color x:Key="InputBorderColor">#5D4A31</Color>
        <Color x:Key="AccentColor">#C9A55F</Color>
        <Color x:Key="AccentBrightColor">#E3C987</Color>
        <Color x:Key="AccentDarkColor">#604722</Color>
        <Color x:Key="MutedTextColor">#A79C89</Color>
        <Color x:Key="MainTextColor">#E8DFCF</Color>
        <Color x:Key="WarningColor">#D9934D</Color>
        <Color x:Key="SuccessColor">#88A96F</Color>
        <Color x:Key="DangerColor">#B76755</Color>

        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}" />
        <SolidColorBrush x:Key="RaisedPanelBrush" Color="{StaticResource RaisedPanelColor}" />
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="{StaticResource InputBackgroundColor}" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource InputBorderColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="AccentBrightBrush" Color="{StaticResource AccentBrightColor}" />
        <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentDarkColor}" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}" />
        <SolidColorBrush x:Key="MainTextBrush" Color="{StaticResource MainTextColor}" />
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />

        <LinearGradientBrush x:Key="WindowBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0D0C09" Offset="0" />
            <GradientStop Color="#090806" Offset="0.55" />
            <GradientStop Color="#100D09" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SidebarBackgroundBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#17130F" Offset="0" />
            <GradientStop Color="#100E0B" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="HeroBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#251D13" Offset="0" />
            <GradientStop Color="#15110D" Offset="0.48" />
            <GradientStop Color="#0E0C09" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PanelGradientBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#211B14" Offset="0" />
            <GradientStop Color="#15110D" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="OrnamentLineBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#006B5433" Offset="0" />
            <GradientStop Color="#8F9C7744" Offset="0.18" />
            <GradientStop Color="#D2C19B62" Offset="0.5" />
            <GradientStop Color="#8F9C7744" Offset="0.82" />
            <GradientStop Color="#006B5433" Offset="1" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PrimaryButtonBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#76592C" Offset="0" />
            <GradientStop Color="#493318" Offset="1" />
        </LinearGradientBrush>

        <DrawingBrush x:Key="StoneTextureBrush"
                      TileMode="Tile"
                      Viewport="0,0,64,64"
                      ViewportUnits="Absolute"
                      Viewbox="0,0,64,64"
                      ViewboxUnits="Absolute"
                      Opacity="0.2">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="Transparent" Geometry="M0,0 H64 V64 H0 Z" />
                    <GeometryDrawing Geometry="M-8,20 L28,-4 M19,68 L68,19">
                        <GeometryDrawing.Pen>
                            <Pen Brush="#345B4731" Thickness="1" />
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                    <GeometryDrawing Geometry="M5,45 L22,36 M42,12 L59,3">
                        <GeometryDrawing.Pen>
                            <Pen Brush="#2A8D714B" Thickness="1" />
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#286E5435" Geometry="M10,12 L11,12 L11,13 L10,13 Z M51,43 L52,43 L52,44 L51,44 Z M31,57 L32,57 L32,58 L31,58 Z" />
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <Style TargetType="{x:Type Window}">
            <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        </Style>

        <Style TargetType="{x:Type UserControl}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <Style TargetType="{x:Type ContentControl}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        </Style>

        <Style x:Key="PageTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Palatino Linotype" />
            <Setter Property="FontSize" Value="31" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Palatino Linotype" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        </Style>

        <Style x:Key="EyebrowTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,7" />
        </Style>

        <Style x:Key="PanelCardStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource PanelGradientBrush}" />
            <Setter Property="BorderBrush" Value="#725A38" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="2" />
            <Setter Property="Padding" Value="22" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>

        <Style x:Key="InsetPanelStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="#AA0D0B08" />
            <Setter Property="BorderBrush" Value="#51412C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="1" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="OrnamentRuleStyle" TargetType="{x:Type Border}">
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="{StaticResource OrnamentLineBrush}" />
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource PanelGradientBrush}" />
            <Setter Property="BorderBrush" Value="#766044" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="Padding" Value="16,9" />
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="1">
                                <Grid>
                                    <Border Margin="2" BorderBrush="#2DFFFFFF" BorderThickness="1" />
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"
                                                      RecognizesAccessKey="True" />
                                </Grid>
                            </Border>
                            <Border x:Name="TopGlint" Height="1" Margin="4,1,4,0" VerticalAlignment="Top" Background="#55E9D19A" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource AccentBrightBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="OuterBorder" Property="Opacity" Value="0.78" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.38" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#FFF3D0" />
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrightBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource AccentDarkBrush}" />
            <Setter Property="MinHeight" Value="36" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
            <Setter Property="Padding" Value="7" />
            <Setter Property="MinHeight" Value="36" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
            <Setter Property="Padding" Value="9,7" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#4B3820" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrightBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#624824" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Grid Width="18" Height="18">
                                    <Border x:Name="CheckFrame"
                                            Background="{StaticResource InputBackgroundBrush}"
                                            BorderBrush="{StaticResource InputBorderBrush}"
                                            BorderThickness="1" />
                                    <Path x:Name="CheckMark"
                                          Data="M3,9 L7,13 L15,4"
                                          Stroke="{StaticResource AccentBrightBrush}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Square"
                                          StrokeEndLineCap="Square"
                                          Opacity="0" />
                                </Grid>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckFrame" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="CheckFrame" Property="Background" Value="#50391C" />
                                <Setter TargetName="CheckMark" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckFrame" Property="BorderBrush" Value="{StaticResource AccentBrightBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.42" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Background" Value="#F01A1510" />
            <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentDarkBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="9,6" />
        </Style>
    </Application.Resources>
</Application>
